# Core detection/tracking/re-id (M1/M2)
opencv-python
numpy
scipy
torch
torchvision
ultralytics
gdown
torchreid
lap
tensorboard

# Demographics (M3) - mivolo-body backend (transformers-hosted mivolo_v2)
# Pinned per https://huggingface.co/iitolstykh/mivolo_v2's own requirements -
# newer transformers has broken the custom modeling code in testing.
transformers==4.51.0
accelerate==1.8.1

# Demographics (M3) - alerts.py (Ollama HTTP client)
requests

# Heatmap hosting (M4) - cloudinary_upload.py, used by render_tracked_video.py --persist
cloudinary
python-dotenv

# NOTE: the `mivolo` package (needed for both demographics backends, and for
# the mivolo-official backend's Detector/MiVOLO classes) is not on PyPI and
# is NOT installed by this file - see README.md for the exact install
# command, which needs a setuptools workaround and must run after this file.
