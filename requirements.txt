fastapi==0.115.9  # pinned by chromadb 1.0.9's own dependency requirement
uvicorn==0.34.0
psycopg2-binary==2.9.10
pydantic==2.10.4
openai==1.58.1
python-dotenv==1.0.1

# For chatbot.py's search_visual tool (CLIP text encoder + Chroma client).
# torch is installed separately in the Dockerfile from the CPU-only index -
# the default PyPI wheel pulls in CUDA and balloons the image for no benefit
# in a container that only runs the text encoder.
transformers==5.14.1
pillow==12.2.0
numpy==2.4.4
chromadb==1.0.9
posthog<6  # ChromaDB 1.0.9 telemetry API is incompatible with posthog 6+
